import styled from 'styled-components'
import { space, layout, typography, color } from 'styled-system'

const Text = styled.p`
  margin: 0;
  ${space}
  ${layout}
  ${typography}
  ${color}
`

export default Text