import styled from 'styled-components'
import {
  space,
  layout,
  typography,
  color,
  position,
  shadow,
  flexbox,
  border,
} from 'styled-system'

const Box = styled.div`
  ${space}
  ${layout}
  ${typography}
  ${color}
  ${position}
  ${shadow}
  ${flexbox}
  ${border}
`

export default Box
